<h1>Welcome to Muscle Memory</h1>
<p>This is the top page of my portfolio.</p>

<%= form_with(url: search_posts_path, method: :get, local: true) do %>
  <div class="field">
    <%= label_tag :query, "検索" %>
    <%= text_field_tag :query, params[:query] %>
  </div>
  <div class="actions">
    <%= submit_tag "検索" %>
  </div>
<% end %>