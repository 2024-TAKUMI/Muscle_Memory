<h1>上半身の記事一覧</h1>
<% @posts.each do |post| %>
  <div class="card mb-3">
    <%= image_tag url_for(post.image), class: "card-img-top", alt: post.title if post.image.attached? %>
    <div class="card-body">
      <h5 class="card-title"><%= link_to post.title, post_path(post) %></h5>
      <p class="card-text"><%= post.body.truncate(100) %></p>
      <p class="card-text"><small class="text-muted"><%= post.created_at.strftime("%Y/%m/%d %H:%M") %></small></p>
    </div>
  </div>
<% end %>
