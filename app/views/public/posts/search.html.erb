<h1><%= @keyword %> の検索結果</h1>

<% if @posts.any? %>
  <% @posts.each do |post| %>
    <div class="post">
      <h3><%= link_to post.title, post_path(post) %></h3>
      <p><%= truncate(post.body, length: 100) %></p>
    </div>
  <% end %>
<% else %>
  <p>該当する投稿が見つかりませんでした。</p>
<% end %>

<button class="btn btn-secondary back-button">戻る</button>
